{
  "name": "api-gateway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node ./build/index.js",
    "build": "tsc",
    "supergraph:gen": "rover supergraph compose --config supergraph.yaml --output supergraph.graphql"
  },
  "dependencies": {
    "@apollo/gateway": "^2.8.4",
    "apollo-server": "^3.13.0"
  },
  "devDependencies": {
    "@apollo/rover": "^0.26.0",
    "typescript": "^5.5.4"
  }
}
